{
 "binary_version": "2.1.280",
 "cols": 120,
 "harness": "claude-code",
 "notes": "Interrupt before the first token: no marker is painted, the prompt's echo disappears and the prompt is put back in the composer; chat clears it (Ctrl-E, Ctrl-K…, Ctrl-U…) and the next prompt is answered alone. The transcript keeps only the cancelled prompt's user entry. Recorded with `HW_RECORD_INTERRUPT=1 go test ./pkg/chat -run RecordInterrupt` (pkg/chat/interrupt_record_test.go), interrupting through chat.Conversation.Interrupt: the real claude against a local Messages API, in /tmp/hw-rebake/interrupt-before-first-token with a fresh CLAUDE_CONFIG_DIR (onboarding done, the directory trusted, permissions.defaultMode bypassPermissions, accepted) and a placeholder ANTHROPIC_AUTH_TOKEN, so no account or tokens are involved.",
 "recorded_at": "2026-09-23T11:12:16.823573Z",
 "rows": 40
}
