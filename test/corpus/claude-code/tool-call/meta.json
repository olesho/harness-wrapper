{
  "harness": "claude-code",
  "binary_version": "2.1.270",
  "recorded_at": "2026-09-13T21:48:24.907041Z",
  "cols": 120,
  "rows": 40,
  "notes": "A completed TOOL-USING turn on Claude Code 2.1.270: the prompt asks for a Bash listing of the working directory, so the frame carries a \"Listed 1 directory\" tool block, the answer (2 .go files) and the end-of-turn summary. The footer reads \"auto mode on\". Recorded with `make rebake-corpus HARNESS=claude SCENARIO=tool-call WORKDIR=/tmp/hw-rebake/tool-call` from a fresh `git init` directory (holding add.go and sub.go) and an isolated CLAUDE_CONFIG_DIR (onboarding done, permissions.defaultMode auto, static CLAUDE_CODE_OAUTH_TOKEN, every inherited CLAUDE* session marker removed), so the recording carries no machine path, account setup or nesting warning. The script's answer_dialog step accepted the folder-trust dialog, which claude reset once, as it does to the first Enter. expected.txt is the last live frame rendered by tmux (bytes.raw replayed up to claude's teardown), not by the emulator under test."
}
